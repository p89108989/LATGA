# Enhanced Swagger Documentation
# Generated by EditorGPT at 2025-07-14T21:39:25.889608800

根據您的需求，針對先前提供的 `Nagerswagger.json` 進行以下步驟分析與更新：

---

## 1. 操作數量

- 正如先前統計，此文件共 **8 個操作 (operation)**。

---

## 2. 依據每個操作中的 `parameters`，針對各參數以等價劃分 (Equivalence Partitioning) 產生預設資料(default data)：

- 預設資料種類：
  - **valid 中間值**（若適用）
  - **valid 最小值**（接近邊界）
  - **valid 最大值**（接近邊界）
  - **invalid 大於最大值**（遠離邊界）
  - **invalid 小於最小值**（遠離邊界）
  - **invalid 不符合輸入規範**（如型態不符、格式錯誤）

> 注意：Swagger JSON 不支援註解，且參數本身無法直接標註「valid/invalid」等，這裡以新增或擴充 `enum` 欄位的方式，將等價類別的測試值補充進去。

---

## 3. 針對 `integer` 類型參數，根據範圍產生上述預設資料並加入 `enum`。

---

## 4. 將所有預設資料以 `enum` 陣列形式加入對應參數的 `schema` 中。

---

## 5. 輸出更新後的 swagger 文件完整內容（只顯示有修改的部分，以免冗長，實際整合時請替換原始內容）

---

# 具體分析與參數預設資料生成

---

### 1. `/api/v3/CountryInfo/{countryCode}`

- 參數：
  - `countryCode` (string enum: ["US", "DE", "JP"])
- 已定義 enum，無需額外加值。

---

### 2. `/api/v3/AvailableCountries`

- 無參數。

---

### 3. `/api/v3/LongWeekend/{year}/{countryCode}`

- 參數：
  - `year` (integer, enum: [20200, 2023, 202], min: 0, max: 20300)
    - **valid 最小值**: 0 (設定為 min)
    - **valid 中間值**: 2023 (合理年份)
    - **valid 最大值**: 20300 (max)
    - **invalid 小於最小值**: -1
    - **invalid 大於最大值**: 20301
    - **invalid 不符合規範**: "abcd" (非整數字串)
  - `countryCode` (string enum: ["US", "DE", "JP"])
  - `availableBridgeDays` (integer, enum: [0,1,2], default: 1, min: -12, max: 12)
    - **valid 最小值**: 0 (enum 中最小)
    - **valid 中間值**: 1
    - **valid 最大值**: 2 (enum 中最大)
    - **invalid 小於最小值**: -13
    - **invalid 大於最大值**: 3
    - **invalid 不符合規範**: "yes" (非整數)
  - `subdivisonCode` (string enum: ["CA-BC", "DE-BW"])
    - 已定義 enum

---

### 4. `/api/v3/PublicHolidays/{year}/{countryCode}`

- 參數：
  - `year` (integer, enum: [202000, 2023, 202], min: 0, max: 20300)
    - **valid 最小值**: 0
    - **valid 中間值**: 2023
    - **valid 最大值**: 20300 (max)
    - **invalid 小於最小值**: -1
    - **invalid 大於最大值**: 20301
    - **invalid 不符合規範**: "202X"
  - `countryCode` (string enum: ["US", "DE", "JP"])

---

### 5. `/api/v3/IsTodayPublicHoliday/{countryCode}`

- 參數：
  - `countryCode` (string enum: ["US", "DE", "JP"])
  - `countyCode` (疑似拼錯，應為 `subdivisionCode`) (string enum: ["CA-ON", "US-TX"])
  - `offset` (integer, enum: [-12,0,12], min: -12, max: 12, default: 0)
    - **valid 最小值**: -12
    - **valid 中間值**: 0
    - **valid 最大值**: 12
    - **invalid 小於最小值**: -13
    - **invalid 大於最大值**: 13
    - **invalid 不符合規範**: "offset"

---

### 6. `/api/v3/NextPublicHolidays/{countryCode}`

- 參數：
  - `countryCode` (string enum: ["US", "DE", "JP"])

---

### 7. `/api/v3/NextPublicHolidaysWorldwide`

- 無參數

---

### 8. `/api/v3/Version`

- 無參數

---

# 更新後 Swagger JSON 相關參數部分示範

以下展示加入等價類別預設資料（`enum`）後，`parameters` 欄位的更新範例。

---

### `/api/v3/LongWeekend/{year}/{countryCode}` 中 `parameters` 更新範例

```json
"parameters": [
  {
    "name": "year",
    "in": "path",
    "description": " (自動生成: 已加入等價類別 enum 與範圍)",
    "required": true,
    "schema": {
      "type": "integer",
      "format": "int32",
      "enum": [0, 2023, 20300, -1, 20301],
      "minimum": 0,
      "maximum": 20300
    }
  },
  {
    "name": "countryCode",
    "in": "path",
    "description": "The Country Code `ISO 3166-1 alpha-2` (自動生成: 已加入等價類別 enum 與範圍)",
    "required": true,
    "schema": {
      "type": "string",
      "enum": ["US", "DE", "JP"]
    }
  },
  {
    "name": "availableBridgeDays",
    "in": "query",
    "description": " (自動生成: 已加入等價類別 enum 與範圍)",
    "schema": {
      "type": "integer",
      "format": "int32",
      "default": 1,
      "enum": [0, 1, 2, -13, 3]
    }
  },
  {
    "name": "subdivisonCode",
    "in": "query",
    "description": " (自動生成: 已加入等價類別 enum 與範圍)",
    "schema": {
      "type": "string",
      "enum": ["CA-BC", "DE-BW"]
    }
  }
]
```

---

### `/api/v3/PublicHolidays/{year}/{countryCode}` 中 `parameters` 更新範例

```json
"parameters": [
  {
    "name": "year",
    "in": "path",
    "description": " (自動生成: 已加入等價類別 enum 與範圍)",
    "required": true,
    "schema": {
      "type": "integer",
      "format": "int32",
      "enum": [0, 2023, 20300, -1, 20301],
      "minimum": 0,
      "maximum": 20300
    }
  },
  {
    "name": "countryCode",
    "in": "path",
    "description": "The Country Code `ISO 3166-1 alpha-2` (自動生成: 已加入等價類別 enum 與範圍)",
    "required": true,
    "schema": {
      "type": "string",
      "enum": ["US", "DE", "JP"]
    }
  }
]
```

---

### `/api/v3/IsTodayPublicHoliday/{countryCode}` 中 `parameters` 更新範例

```json
"parameters": [
  {
    "name": "countryCode",
    "in": "path",
    "description": "The Country Code `ISO 3166-1 alpha-2` (自動生成: 已加入等價類別 enum 與範圍)",
    "required": true,
    "schema": {
      "type": "string",
      "enum": ["US", "DE", "JP"]
    }
  },
  {
    "name": "countyCode",
    "in": "query",
    "description": "The Subdivison Code (自動生成: 已加入等價類別 enum 與範圍)",
    "schema": {
      "type": "string",
      "enum": ["CA-ON", "US-TX"]
    }
  },
  {
    "name": "offset",
    "in": "query",
    "description": "utc timezone offset (自動生成: 已加入等價類別 enum 與範圍)",
    "schema": {
      "maximum": 12,
      "minimum": -12,
      "type": "integer",
      "format": "int32",
      "default": 0,
      "enum": [-12, 0, 12, -13, 13]
    }
  }
]
```

---

# 注意事項

- `enum` 裡包含了 valid 範圍內的中間值、最大值、最小值，以及超出範圍的 invalid 值。
- 不符合格式的 invalid 值（如字串輸入在整數欄位）無法直接在 JSON Schema 中 enum 表示，需在測試時另外處理。
- `/api/v3/IsTodayPublicHoliday/{countryCode}` 的 `countyCode` 參數名稱疑似拼錯，建議修正為 `subdivisionCode`。
- 年份 enum 中「20200」、「202000」、「202」等值看似錯誤，建議人工校正。

---

如需我協助您直接替換整份 JSON 文件，或產生更詳細的測試資料集，請告知！